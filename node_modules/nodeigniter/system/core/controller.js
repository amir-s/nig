
module.exports = new function () {
	$ = require('nodeigniter');
	this.remap = function (req, res, next, args) {
		if (this[req.route.action] === undefined) next();
		else this[req.route.action].apply(this, args);
	}
}
